package enums;

public enum Databases {       // internally Databases is represented as class
	MYSQL,SQLSERVER,ORACLE   // all these constant represent as object or reference of that(Databases) class and  these are public static final 

}
